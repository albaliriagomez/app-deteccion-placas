allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"

subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

// Fix para plugins que no declaran namespace (necesario con AGP 8+)
subprojects { subproject ->
    subproject.plugins.withId("com.android.library") {
        def android = subproject.extensions.findByName("android")
        if (android != null && (android.namespace == null || android.namespace == "")) {
            android.namespace = "com.example.${subproject.name.replace('-', '_')}"
        }
    }

    subproject.plugins.withId("com.android.application") {
        def android = subproject.extensions.findByName("android")
        if (android != null && (android.namespace == null || android.namespace == "")) {
            android.namespace = "com.example.${subproject.name.replace('-', '_')}"
        }
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}